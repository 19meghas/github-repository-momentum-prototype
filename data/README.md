# Data Notes

This project uses public GitHub sample repository activity data queried through Google BigQuery.

No large raw datasets are stored in this repository.

SQL queries are provided in the `queries/` folder so the analysis can be reproduced from the public BigQuery source.